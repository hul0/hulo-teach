import ProgressPageClient from "./ProgressPageClient"

export const metadata = {
  title: "Progress",
  description: "Track completed articles and quizzes.",
}

export default function ProgressPage() {
  return <ProgressPageClient />
}
